<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yapps.gallery">

    <uses-permission-sdk-23 android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission-sdk-23 android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_gallery"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_gallery"
        android:supportsRtl="true"
        android:theme="@style/AppThemeDark">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".easyloader.LoaderActivity"/>
        <activity android:name=".imagewall.ui.ImageGridActivity"/>
        <activity android:name=".imagewall.ui.ImageDetailActivity"/>
    </application>

</manifest>